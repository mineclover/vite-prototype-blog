import styled from 'styled-components';
import { Pretendard, Jamsil } from '@/global/styleUtils';
import colors from '@/global/colors';

type Props = {};

const index = (props: Props) => {
  return <Pretendard>야 왜 안됨</Pretendard>;
};

export default index;
